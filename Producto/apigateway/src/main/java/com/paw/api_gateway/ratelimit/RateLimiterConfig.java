package com.paw.api_gateway.ratelimit;

import org.springframework.context.annotation.Configuration;

@Configuration
public class RateLimiterConfig {
    // Reserved for the next step: per-IP or per-user rate limits at the gateway edge.
}
